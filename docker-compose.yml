# ModelScope MiniLab / 智模工坊 —— 生产/服务器部署
#
# 用法：
#   1. 把本文件和 .env 放到服务器同一目录（不需要源码，镜像从 Docker Hub 拉）
#   2. cp docker-compose.env.example .env  并填好真实密钥
#   3. docker compose up -d
#   4. 查看：docker compose logs -f backend
#
# 默认启用 backend + web + redis，数据库用 SQLite（落到卷里，重启不丢）。
# 想用 MySQL：docker compose --profile mysql up -d，并按 .env 注释改 DATABASE_URL。
#
# web 容器内已把 /api/ 反代到 backend 服务：宿主机 Nginx 只需一条
# location / 指向 web（默认 127.0.0.1:8080，可用 WEB_HTTP_PORT 改端口），
# 前端与接口即同源，无需配置 CORS。

services:
  backend:
    image: yunluoxincheng/modelscope-minilab-backend:latest
    container_name: minilab-backend
    env_file:
      - ./.env
    environment:
      # 覆盖镜像里的默认值，强制连到本 compose 网络里的 redis。
      - REDIS_URL=redis://redis:6379/0
    ports:
      - "8000:8000"
    volumes:
      - minilab-data:/app/data
    depends_on:
      redis:
        condition: service_healthy
      mysql:
        condition: service_healthy
        required: false   # 没启用 mysql profile 时跳过
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8000/api/health"]
      interval: 30s
      timeout: 5s
      start_period: 40s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: minilab-redis
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  web:
    image: yunluoxincheng/modelscope-minilab-web:latest
    container_name: minilab-web
    ports:
      - "${WEB_HTTP_PORT:-8080}:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1/"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

  mysql:
    profiles: ["mysql"]
    image: mysql:8.0
    container_name: minilab-mysql
    env_file:
      - ./.env
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-change-me}
      - MYSQL_DATABASE=${MYSQL_DATABASE:-minilab}
      - MYSQL_USER=${MYSQL_USER:-minilab}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD:-change-me}
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    volumes:
      - mysql-data:/var/lib/mysql
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-change-me}"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  minilab-data:
  redis-data:
  mysql-data:
